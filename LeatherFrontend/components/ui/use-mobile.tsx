// Re-export the canonical `useIsMobile` from `hooks`.
// Keeps `@/components/ui/use-mobile` import path functional while
// centralizing the implementation in `hooks/use-mobile`.

export { useIsMobile } from '@/hooks/use-mobile'
